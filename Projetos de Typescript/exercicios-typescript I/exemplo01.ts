import * as rs from 'readline-sync';

const nome = rs.question('Type your Name, please...');

const message: string = "Hello! Your name is:" + nome + "!";
console.log(message);

// Exemplo de criar classe
class juff{
    constructor(){
        
    }
}